---
# === YAML Frontmatter (машиночитаемые метаданные) ===

feature_id: "F002"
feature_name: "web-ui"
title: "Веб-интерфейс для Free AI Selector"
created: "2025-12-25"
author: "AI (Analyst)"
type: "prd"
status: "PRD_READY"
version: 1
mode: "FEATURE"

related_features: []
services: ["free-ai-selector-business-api"]
requirements_count: 8
---

# PRD: Веб-интерфейс "Рейтинг бесплатных ИИ моделей"

**Feature ID**: F002
**Версия**: 1.0
**Дата**: 2025-12-25
**Автор**: AI Agent (Аналитик)
**Статус**: PRD_READY

---

## 1. Обзор

### 1.1 Проблема

Сейчас пользователи Free AI Selector имеют два способа взаимодействия:
1. **Telegram Bot** — требует установки Telegram
2. **Swagger UI (/docs)** — технический интерфейс для разработчиков

На главной странице nginx-proxy (`https://95.142.37.184/`) есть ссылка "Рейтинг бесплатных ИИ моделей", которая ведёт на Swagger. Это не удобно для обычных пользователей, которые хотят:
- Быстро отправить запрос к AI
- Посмотреть статистику моделей
- Протестировать доступность провайдеров

### 1.2 Решение

Создать **минимальный веб-интерфейс** — одну страницу в стилистике существующих сервисов (минимализм: белый фон, карточки с тенью, синие кнопки).

Страница объединит три функции:
1. **Чат с AI** — форма для отправки промпта
2. **Статистика моделей** — таблица с рейтингом надёжности
3. **Тестирование провайдеров** — проверка работоспособности всех 6 провайдеров

**Ключевое ограничение**: Используются ТОЛЬКО существующие эндпоинты Business API. Новые эндпоинты НЕ добавляются.

### 1.3 Целевая аудитория

| Сегмент | Описание | Потребности |
|---------|----------|-------------|
| Обычные пользователи | Хотят задать вопрос AI через браузер | Простая форма, понятный интерфейс |
| Администраторы | Мониторят работоспособность системы | Статистика, тестирование провайдеров |

### 1.4 Ценностное предложение

- Не требует установки Telegram
- Работает в любом браузере
- Минималистичный интерфейс без сложностей
- Весь функционал на одной странице

---

## 2. Функциональные требования

### 2.1 Core Features (Must Have)

| ID | Название | Описание | Критерий приёмки |
|----|----------|----------|------------------|
| FR-001 | Форма отправки промпта | Textarea для ввода текста + кнопка "Отправить". Вызывает `POST /api/v1/prompts/process` | Пользователь вводит текст, нажимает кнопку, получает ответ AI |
| FR-002 | Отображение ответа AI | Показывает: текст ответа, название модели, провайдер, время ответа | После успешного запроса отображается блок с результатом |
| FR-003 | Таблица статистики моделей | Список моделей с reliability_score, success_rate, is_active. Вызывает `GET /api/v1/models/stats` | Таблица отображает все модели, отсортированные по reliability_score |
| FR-004 | Кнопка обновления статистики | Кнопка "Обновить" для перезагрузки статистики | Нажатие обновляет данные без перезагрузки страницы |

### 2.2 Important Features (Should Have)

| ID | Название | Описание | Критерий приёмки |
|----|----------|----------|------------------|
| FR-010 | Тестирование провайдеров | Кнопка "Тестировать все" + отображение результатов. Вызывает `POST /api/v1/providers/test` | Показывает статус каждого провайдера (успех/ошибка) и время ответа |
| FR-011 | Индикатор состояния системы | В footer/header показывать статус системы. Вызывает `GET /health` | Зелёный/красный индикатор "Онлайн"/"Оффлайн" |
| FR-012 | Индикатор загрузки | Показывать loading spinner при выполнении запросов | Spinner отображается во время ожидания ответа API |

### 2.3 Nice to Have (Could Have)

| ID | Название | Описание | Критерий приёмки |
|----|----------|----------|------------------|
| FR-020 | Медали для топ-моделей | Показывать 🥇🥈🥉 для топ-3 моделей в таблице | Визуальное выделение лучших моделей |

---

## 3. User Stories

### US-001: Отправка запроса к AI

**Как** обычный пользователь
**Я хочу** ввести вопрос и получить ответ от AI
**Чтобы** быстро решить свою задачу без установки Telegram

**Критерии приёмки:**
- [ ] Форма с textarea (placeholder: "Введите ваш запрос...")
- [ ] Кнопка "Отправить" (синяя, как на smi-convert)
- [ ] Результат показывает: ответ, модель, провайдер, время
- [ ] При ошибке показывается понятное сообщение

**Связанные требования:** FR-001, FR-002, FR-012

---

### US-002: Просмотр статистики моделей

**Как** администратор
**Я хочу** видеть рейтинг надёжности AI моделей
**Чтобы** понимать какие провайдеры работают лучше

**Критерии приёмки:**
- [ ] Таблица с колонками: Модель, Провайдер, Score, Статус
- [ ] Сортировка по reliability_score (лучшие сверху)
- [ ] Кнопка "Обновить" для обновления данных

**Связанные требования:** FR-003, FR-004, FR-020

---

### US-003: Диагностика провайдеров

**Как** администратор
**Я хочу** проверить работоспособность всех AI провайдеров
**Чтобы** выявить проблемы с конкретными сервисами

**Критерии приёмки:**
- [ ] Кнопка "Тестировать все"
- [ ] Результаты: ✅ успех + время или ❌ ошибка + причина
- [ ] Итоговая статистика: "5/6 работают"

**Связанные требования:** FR-010, FR-012

---

## 4. UI/UX требования

### 4.1 Экраны и интерфейсы

| ID | Экран | Описание | Приоритет |
|----|-------|----------|-----------|
| UI-001 | Единственная страница | Одностраничный интерфейс с тремя секциями в карточках | Must |

### 4.2 Стилистика (согласно примерам)

**Общие принципы:**
- Белый фон (#ffffff)
- Карточки с лёгкой тенью (box-shadow)
- Системный шрифт (sans-serif)
- Синие кнопки акцента (#0066ff или подобный)
- Центрированный контент (max-width: ~800px)
- Русский язык интерфейса

**Структура страницы:**
```
┌─────────────────────────────────────────────────────┐
│  ЗАГОЛОВОК: "Рейтинг бесплатных ИИ моделей"         │
│  Подзаголовок: "Автоматический выбор лучшей модели" │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │ СЕКЦИЯ 1: Чат с AI                          │    │
│  │ [textarea]                                   │    │
│  │ [Отправить]                                  │    │
│  │ Результат: ...                               │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ СЕКЦИЯ 2: Статистика моделей [🔄 Обновить]  │    │
│  │ ┌───┬──────────┬───────────┬───────┬─────┐  │    │
│  │ │ # │ Модель   │ Провайдер │ Score │ ... │  │    │
│  │ └───┴──────────┴───────────┴───────┴─────┘  │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ СЕКЦИЯ 3: Тестирование [Тестировать все]    │    │
│  │ Результаты: ✅ Provider 1 - 1.2s            │    │
│  │            ❌ Provider 2 - ошибка           │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ─────────────────────────────────────────────      │
│  Статус: 🟢 Система онлайн                          │
└─────────────────────────────────────────────────────┘
```

### 4.3 Требования к доступности

- [ ] Интерфейс на русском языке
- [ ] Респонсивный дизайн (работает на мобильных)
- [ ] Кнопки с понятными текстами

---

## 5. Нефункциональные требования

### 5.1 Производительность

| ID | Метрика | Требование | Измерение |
|----|---------|------------|-----------|
| NF-001 | Время загрузки страницы | < 2s | Браузерные DevTools |

### 5.2 Совместимость

| ID | Требование | Описание |
|----|------------|----------|
| NF-010 | Браузеры | Chrome, Firefox, Safari, Edge (последние версии) |
| NF-011 | Мобильные устройства | Респонсивный дизайн |

### 5.3 Интеграция

| ID | Требование | Описание |
|----|------------|----------|
| NF-020 | Static Files | Размещение в Business API как static files (FastAPI StaticFiles) |
| NF-021 | ROOT_PATH | Корректная работа за nginx-proxy с ROOT_PATH |

---

## 6. Технические ограничения

### 6.1 Обязательные технологии

- **Frontend**: Vanilla HTML/CSS/JS (без фреймворков)
- **Backend**: Существующий FastAPI Business API
- **Размещение**: Static files внутри Business API

### 6.2 API эндпоинты (используемые)

| Эндпоинт | Метод | Назначение |
|----------|-------|------------|
| `/api/v1/prompts/process` | POST | Обработка промпта |
| `/api/v1/models/stats` | GET | Статистика моделей |
| `/api/v1/providers/test` | POST | Тестирование провайдеров |
| `/health` | GET | Проверка здоровья |

### 6.3 Ограничения

- Новые API эндпоинты НЕ добавляются
- Использовать только существующий функционал Business API
- Минимальный размер файлов (без node_modules, npm, webpack)

---

## 7. Допущения и риски

### 7.1 Допущения

| # | Допущение | Влияние если неверно |
|---|-----------|---------------------|
| 1 | FastAPI поддерживает StaticFiles mounting | Потребуется альтернативное решение |
| 2 | CORS настроен для same-origin запросов | API вызовы будут блокироваться |

### 7.2 Риски

| # | Риск | Вероятность | Влияние | Митигация |
|---|------|-------------|---------|-----------|
| 1 | ROOT_PATH не учитывается в JS | Med | High | Использовать относительные пути или BASE_URL |
| 2 | Конфликт с существующими маршрутами | Low | Med | Проверить роутинг перед реализацией |

---

## 8. Открытые вопросы

| # | Вопрос | Статус | Ответственный | Решение |
|---|--------|--------|--------------|---------|
| - | Нет открытых вопросов | — | — | — |

---

## 9. Глоссарий

| Термин | Определение |
|--------|-------------|
| reliability_score | Метрика надёжности модели: `(success_rate × 0.6) + (speed_score × 0.4)` |
| Static Files | Статические файлы (HTML, CSS, JS), отдаваемые FastAPI |
| ROOT_PATH | Префикс пути для работы за reverse proxy (nginx) |

---

## 10. Изменения в существующем коде

### 10.1 Файлы для создания

| Файл | Описание |
|------|----------|
| `app/static/index.html` | Главная страница веб-интерфейса |
| `app/static/style.css` | Стили (минимализм) |
| `app/static/app.js` | JavaScript логика для API вызовов |

### 10.2 Файлы для модификации

| Файл | Изменение |
|------|-----------|
| `app/main.py` | Добавить StaticFiles mount, изменить `GET /` для редиректа на `/static/index.html` |

---

## 11. Сопоставление функций с эндпоинтами

| UI Функция | API Эндпоинт | Метод | Request | Response |
|------------|--------------|-------|---------|----------|
| Отправка промпта | `/api/v1/prompts/process` | POST | `{"prompt": "text"}` | `{response, selected_model, provider, response_time_seconds}` |
| Статистика | `/api/v1/models/stats` | GET | — | `{models: [{name, provider, reliability_score, is_active}]}` |
| Тестирование | `/api/v1/providers/test` | POST | — | `{total_providers, successful, failed, results}` |
| Статус системы | `/health` | GET | — | `{status, service, version}` |

---

## 12. История изменений

| Версия | Дата | Автор | Изменения |
|--------|------|-------|-----------|
| 1.0 | 2025-12-25 | AI Analyst | Первоначальная версия PRD |

---

## Качественные ворота

### PRD_READY Checklist

- [x] Все секции заполнены
- [x] Требования имеют уникальные ID (FR-*, NF-*, UI-*)
- [x] Критерии приёмки определены для каждого требования
- [x] User stories связаны с требованиями
- [x] Нет блокирующих открытых вопросов
- [x] Риски идентифицированы и имеют план митигации
- [x] Сопоставление UI функций с существующими API эндпоинтами
