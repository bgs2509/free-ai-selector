---
feature_id: "F002"
feature_name: "web-ui"
title: "План реализации: Веб-интерфейс для Free AI Selector"
created: "2025-12-25"
author: "AI (Architect)"
type: "plan"
status: "PLAN_APPROVED"
version: 1
mode: "FEATURE"
---

# План фичи: F002 Web UI

**Feature ID**: F002
**Дата**: 2025-12-25
**Автор**: AI Agent (Архитектор)
**Статус**: PLAN_APPROVED ✅

---

## 1. Обзор

### 1.1 Краткое описание

Добавление минимального веб-интерфейса "Рейтинг бесплатных ИИ моделей" в существующий Business API сервис. Интерфейс размещается как статические файлы (HTML/CSS/JS) и предоставляет доступ к трём функциям:
1. Чат с AI
2. Статистика моделей
3. Тестирование провайдеров

### 1.2 Связь с существующим функционалом

| Новый UI элемент | Существующий API эндпоинт |
|------------------|---------------------------|
| Форма отправки промпта | `POST /api/v1/prompts/process` |
| Таблица статистики | `GET /api/v1/models/stats` |
| Тестирование провайдеров | `POST /api/v1/providers/test` |
| Индикатор статуса | `GET /health` |

**Ключевое ограничение**: Новые API эндпоинты НЕ добавляются. Используется только существующий функционал.

---

## 2. Анализ существующего кода

### 2.1 Затронутые сервисы

| Сервис | Изменения |
|--------|-----------|
| `free-ai-selector-business-api` | Добавление static files, модификация root endpoint |

Остальные сервисы (Data API, Telegram Bot, Health Worker) **не затрагиваются**.

### 2.2 Точки интеграции

**Файл: `services/free-ai-selector-business-api/app/main.py`**

| Строка | Текущий код | Требуемое изменение |
|--------|-------------|---------------------|
| 1-25 | Импорты | Добавить: `StaticFiles`, `RedirectResponse`, `os` |
| 112-121 | Создание FastAPI app | После создания: mount `/static` |
| 258-273 | Root endpoint `@app.get("/")` | Заменить на `RedirectResponse` |

### 2.3 Существующие зависимости

Все необходимые зависимости **уже включены** в проект:

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `fastapi` | Уже есть | `StaticFiles` встроен |
| `starlette` | Уже есть | `RedirectResponse` встроен |

**Новые зависимости не требуются.**

---

## 3. План изменений

### 3.1 Новые компоненты

| # | Компонент | Расположение | Описание |
|---|-----------|--------------|----------|
| 1 | `index.html` | `app/static/index.html` | Главная страница с тремя секциями |
| 2 | `style.css` | `app/static/style.css` | Стили (минимализм: белый фон, карточки, синие кнопки) |
| 3 | `app.js` | `app/static/app.js` | JavaScript для API вызовов |

### 3.2 Модификации существующего кода

| # | Файл | Изменение | Причина |
|---|------|-----------|---------|
| 1 | `app/main.py` | Добавить импорт `StaticFiles`, `RedirectResponse` | Поддержка static files |
| 2 | `app/main.py` | Добавить `app.mount("/static", ...)` после создания app | Сервинг статики |
| 3 | `app/main.py` | Заменить root endpoint на `RedirectResponse(url="/static/index.html")` | Редирект на веб-интерфейс |

### 3.3 Новые зависимости

**Нет** — все необходимые модули уже включены в FastAPI/Starlette.

---

## 4. Детали реализации

### 4.1 Структура static директории

```
services/free-ai-selector-business-api/app/
└── static/                      # НОВАЯ ДИРЕКТОРИЯ
    ├── index.html               # Главная страница
    ├── style.css                # Стили
    └── app.js                   # JavaScript
```

### 4.2 Модификация main.py

**Добавить импорты (после строки 17):**
```python
from fastapi.staticfiles import StaticFiles
from fastapi.responses import RedirectResponse
```

**Добавить mount статики (после строки 125, после `app.add_exception_handler`):**
```python
# =============================================================================
# Static Files
# =============================================================================

# Монтируем статические файлы для веб-интерфейса
static_dir = os.path.join(os.path.dirname(__file__), "static")
if os.path.exists(static_dir):
    app.mount("/static", StaticFiles(directory=static_dir), name="static")
```

**Заменить root endpoint (строки 258-273):**
```python
@app.get("/", include_in_schema=False)
async def root():
    """
    Перенаправление на веб-интерфейс.
    """
    return RedirectResponse(url="/static/index.html")
```

### 4.3 HTML структура (index.html)

```
┌─────────────────────────────────────────────────────┐
│  <header>                                            │
│    <h1>Рейтинг бесплатных ИИ моделей</h1>            │
│    <p>Автоматический выбор лучшей модели</p>        │
│  </header>                                           │
├─────────────────────────────────────────────────────┤
│  <section id="chat">                                 │
│    <h2>Чат с AI</h2>                                │
│    <textarea>...</textarea>                          │
│    <button>Отправить</button>                        │
│    <div id="response">...</div>                      │
│  </section>                                          │
├─────────────────────────────────────────────────────┤
│  <section id="stats">                                │
│    <h2>Статистика моделей <button>🔄</button></h2>   │
│    <table>...</table>                                │
│  </section>                                          │
├─────────────────────────────────────────────────────┤
│  <section id="providers">                            │
│    <h2>Тестирование провайдеров</h2>                │
│    <button>Тестировать все</button>                  │
│    <div id="test-results">...</div>                  │
│  </section>                                          │
├─────────────────────────────────────────────────────┤
│  <footer>                                            │
│    Статус: <span id="status">🟢 Онлайн</span>       │
│  </footer>                                           │
└─────────────────────────────────────────────────────┘
```

### 4.4 CSS стилистика (style.css)

**Согласно PRD (минимализм):**
- Белый фон (`#ffffff`)
- Карточки с тенью (`box-shadow: 0 2px 8px rgba(0,0,0,0.1)`)
- Синие кнопки (`#0066ff`)
- Системный шрифт (`font-family: system-ui, sans-serif`)
- Центрированный контент (`max-width: 800px; margin: 0 auto`)

### 4.5 JavaScript API вызовы (app.js)

**Паттерн API вызовов (same-origin, CORS не требуется):**

```javascript
// Базовый URL — пустая строка для same-origin
const API_BASE = '';

// Универсальная функция API вызова
async function apiCall(endpoint, options = {}) {
    const response = await fetch(`${API_BASE}${endpoint}`, {
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        },
        ...options
    });

    if (!response.ok) {
        const error = await response.json();
        throw new Error(error.detail || 'Ошибка API');
    }

    return response.json();
}

// Функции:
// - sendPrompt(text) → POST /api/v1/prompts/process
// - loadStats() → GET /api/v1/models/stats
// - testProviders() → POST /api/v1/providers/test
// - checkHealth() → GET /health
```

---

## 5. Влияние на существующие тесты

### 5.1 Анализ затронутых тестов

| Тест | Влияние | Действие |
|------|---------|----------|
| `test_root_endpoint` | **Изменится** — теперь ожидается redirect (307) | Обновить тест |
| Остальные тесты | **Не затронуты** | — |

### 5.2 Новые тесты

| Тест | Описание |
|------|----------|
| `test_root_redirects_to_static` | Проверка redirect с `/` на `/static/index.html` |
| `test_static_files_accessible` | Проверка доступности `/static/index.html` |

---

## 6. План интеграции

| # | Шаг | Задачи | Зависимости |
|---|-----|--------|-------------|
| 1 | Создать директорию static | `mkdir app/static` | — |
| 2 | Создать index.html | HTML с 3 секциями | Шаг 1 |
| 3 | Создать style.css | Стили минимализма | Шаг 1 |
| 4 | Создать app.js | API вызовы | Шаг 1 |
| 5 | Модифицировать main.py | Импорты, mount, redirect | Шаги 2-4 |
| 6 | Обновить тесты | Тест root endpoint | Шаг 5 |
| 7 | Локальное тестирование | `make up`, проверка в браузере | Шаги 1-6 |
| 8 | Тестирование за nginx-proxy | Проверка ROOT_PATH | Шаг 7 |

---

## 7. Риски и митигация

| # | Риск | Вероятность | Влияние | Митигация |
|---|------|-------------|---------|-----------|
| 1 | ROOT_PATH не применяется к static | Low | Med | Использовать относительные пути в JS |
| 2 | Конфликт маршрутов | Low | Low | mount статики после создания app, перед роутерами |
| 3 | CORS ошибки | Low | Med | Same-origin запросы, CORS не требуется |
| 4 | Кэширование браузером | Med | Low | Cache-Control headers при необходимости |

---

## 8. Оценка объёма работ

| Компонент | Размер |
|-----------|--------|
| `index.html` | ~100 строк |
| `style.css` | ~150 строк |
| `app.js` | ~120 строк |
| Изменения `main.py` | ~15 строк |
| Обновление тестов | ~20 строк |
| **Итого** | ~400 строк кода |

---

## 9. Чеклист готовности

### PLAN_APPROVED Checklist

- [x] Точки интеграции определены (`main.py` строки 17, 125, 258-273)
- [x] Необходимые изменения описаны (3 новых файла, 1 модификация)
- [x] Существующие тесты проанализированы (1 тест требует обновления)
- [x] Риски идентифицированы (4 риска с митигацией)
- [x] Зависимости проверены (нет новых зависимостей)
- [x] Обратная совместимость обеспечена (API не изменяется)
- [x] **План утверждён пользователем** (2025-12-25)

---

## 10. Следующий шаг

После утверждения плана:

```bash
/aidd-generate
```

---

## Качественные ворота

### PLAN_APPROVED

| Критерий | Статус |
|----------|--------|
| Интеграция определена | ✅ |
| Изменения описаны | ✅ |
| Риски учтены | ✅ |
| **Утверждение пользователя** | ✅ Утверждено |
